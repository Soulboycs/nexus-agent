# 测试矩阵: 高价值测试覆盖矩阵

遵循 `evidence-driven-engineering` 的高价值门禁规范：每个测试必须捕获真实故障，拒绝低价值浅层断言。

---

## 自动化测试矩阵清单

| 测试 ID | 测试目标 / 捕获的真实缺陷 | 测试层级 | 测试文件 | 运行命令 | 判定准则 (Pass Criteria) | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TM-01** | `view_file` 1-indexed 行号精准切片，防止越界 | 单元测试 | `test/fileTools.test.ts` | `npm test` | 行号前缀对齐，切片区间完全吻合，超限友好提示 | 🟢 验证通过 |
| **TM-02** | `replace_file_content` 唯一匹配与模糊换行容错 | 单元测试 | `test/fileTools.test.ts` | `npm test` | 唯一匹配替换成功；多处匹配或未找到时严格报错，杜绝误改 | 🟢 验证通过 |
| **TM-03** | `write_to_file` 覆写安全保护 | 单元测试 | `test/fileTools.test.ts` | `npm test` | `overwrite: false` 时禁止覆盖既有文件并抛出明确异常 | 🟢 验证通过 |
| **TM-04** | Agent ReAct 完整生命周期与多轮事件流 | 集成测试 | `test/agentEngine.test.ts` | `npm test` | 事件流依序产出，最终状态收敛至 `completed` | 🟢 验证通过 |
| **TM-05** | 人机协同 (HITL) 权限审批拦截与恢复 | 集成测试 | `test/agentEngine.test.ts` | `npm test` | 敏感操作进入 `awaiting_confirmation`，批准后恢复，驳回后安全降级 | 🟢 验证通过 |
| **TM-06** | `ToolOrchestrator` 同构切片分区 | 单元测试 | `tests/orchestrator.test.ts` | `bun test` | 连续只读工具聚合为 1 个并发 Batch；写工具严格拆入独立 Batch | 🟡 待执行 |
| **TM-07** | `ToolOrchestrator` 真实并发池上限控制 | 压力/时序测试 | `tests/orchestrator.test.ts` | `bun test` | 6 个并行只读任务真实峰值并发度 > 1 且 <= 4，总耗时接近单任务耗时 | 🟡 待执行 |
| **TM-08** | `query()` 异步生成器平铺状态机与无递归验证 | 集成测试 | `tests/queryEngine.test.ts` | `bun test` | 多轮工具调用平铺赋值流转，验证 `turnCount` 与消息回填 | 🟡 待执行 |
| **TM-09** | `query()` 危险操作审批挂起 | 集成测试 | `tests/queryEngine.test.ts` | `bun test` | 触发 `approval_required`，拦截挂起并在用户 resolve 后恢复 | 🟡 待执行 |
| **TM-10** | `Bun.serve` REST 与 SQLite 增删改查 | 集成测试 | `tests/server.test.ts` | `bun test` | `/health` 探针返回 200，`/api/sessions` 读写无损落库 | 🟡 待执行 |
| **TM-11** | WebSocket `/ws/:sessionId` 双向事件协议 | E2E 集成测试 | `tests/server.test.ts` | `bun test` | 握手返回 `connected` 与 `session_state`，ping/pong 往返 < 50ms | 🟡 待执行 |

---

## 流式输出（Smooth Typewriter Streaming）矩阵 — 2026-09-17 整改新增

| 编号 | 验证边界 | 层级 | 测试文件 | 命令 | 通过标准 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TM-S1** | `pending<=6` 极细逐字节奏（单字/帧下界） | 单元测试 | `tests/stream_pacer.test.ts` | `bun test` | TTFT 旁路后 pending=3 时单帧恰好推进 1 字符 | 🟢 验证通过 |
| **TM-S2** | `setTarget` 整体替换路径（上游修正/截断） | 单元测试 | `tests/stream_pacer.test.ts` | `bun test` | 已展示文本重新锚定到新目标前缀，旧文本零残留；截断后立即完成 | 🟢 验证通过 |
| **TM-S3** | 节奏下界（禁止直出/瞬刷退化） | 单元测试 | `tests/stream_pacer.test.ts` | `bun test` | 120 字符目标 3 帧后仍未展示完（下界），同时首帧有真实推进（上界） | 🟢 验证通过 |
| **TM-S4** | 组件级逐字渲染下界与最终追平 | 组件测试 (happy-dom) | `tests/dom/StreamingText.domtest.tsx` | `npm run test:vitest` | 200 字符流式 2 帧后部分展示、40 帧内完全追平 | 🟢 验证通过 |
| **TM-S5** | 完成态平滑收尾接线（原死路径）+ onComplete | 组件测试 (happy-dom) | `tests/dom/StreamingText.domtest.tsx` | `npm run test:vitest` | 完成瞬间不瞬跳，≤12 帧内收敛并触发 onComplete；历史消息直出无动画 | 🟢 验证通过 |
| **TM-S6** | 禁止合成反引号伪影（closeUnclosedCodeBlocks 已移除） | 组件测试 (happy-dom) | `tests/dom/StreamingText.domtest.tsx` | `npm run test:vitest` | 逐字符喂入含奇数反引号文本，展示恒为目标前缀且永不出现 `\n\`` 伪影 | 🟢 验证通过 |
| **TM-S7** | S2 心跳：5s 断流显示 [Receiving...]，新数据熄灭 | 组件测试 (happy-dom) | `tests/dom/StreamingText.domtest.tsx` | `npm run test:vitest` | 4.5s 不显示、5.5s 显示、新内容到达后熄灭 | 🟢 验证通过 |
| **TM-S8** | 滚动竞态回归：非输入源滚动不得关闭吸底 | DOM 测试 (happy-dom) | `tests/dom/scrollFollower.domtest.ts` | `npm run test:vitest` | 程序滚动/内容增长事件后仍跟随；wheel 上滚禁用；回底恢复；滚动条拖拽禁用；导航键禁用但输入框内忽略 | 🟢 验证通过 |

> 运行器隔离约定：DOM 套件命名为 `*.domtest.tsx`，bun 的发现模式不匹配该后缀，`bun test tests/` 主门禁与 `npm run test:vitest` 互不干扰（实测 bun 仍为 13 个文件）。

---

## 会话存储与 DAG 分叉矩阵 (Session Storage & DAG Engine) — 2026-09-18 对标验收

| 编号 | 验证边界 | 层级 | 测试文件 | 命令 | 通过标准 / 判定准则 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TM-SS01** | 项目工程物理隔离目录转换与落盘 | 单元/集成 | `tests/session_store.test.ts:86-104` | `bun test` | `sanitizePath` 安全替换非字母数字，文件保存在 `projects/<sanitizedProjectDir>/<id>.jsonl` | 🟢 验证通过 |
| **TM-SS02** | 1:1 Claude Code 原生 Entry 格式（无 Line 0 meta 专有方言） | 物理文件验证 | `tests/session_store.test.ts:107-181` | `bun test` | 文件每行严格为标准 Entry（user/assistant/custom-title/ai-title/summary） | 🟢 验证通过 |
| **TM-SS03** | 零重写纯追加 (Crash-Safe Append) 物理副作用断言 | 零信任防假测试 | `tests/session_store.test.ts:183-233` | `bun test` | `spyOn(fs, 'writeFile')` 拦截底层调用，硬性断言针对 session 文件的调用次数严格为 0 | 🟢 验证通过 |
| **TM-SS04** | 掉电尾部半截脏数据容灾与自愈 (Fault Injection) | 对抗性容灾测试 | `tests/session_store.test.ts:235-260` | `bun test` | 注入破损半截 JSON，系统自动跳过损坏行并 100% 完整加载前序有效历史 | 🟢 验证通过 |
| **TM-SS05** | 64KB 常数内存界限 (Head/Tail Lite Discovery) | 性能与边界测试 | `tests/session_store.test.ts:262-276` | `bun test` | 200 行超大文件实测 `readHeadAndTail` 单个窗口严格 `<= 65536` 字节 | 🟢 验证通过 |
| **TM-SS06** | 临时态与占位符消息准入拦截 (isTranscriptMessage) | 负向边界防御 | `tests/session_store.test.ts:323-340` | `bun test` | 空内容且无 block/tools 的临时 assistant 消息拒绝落盘，返回 false | 🟢 验证通过 |
| **TM-SS07** | 多分支 DAG 树形回溯与时序重构 (buildConversationChain) | 算法与逻辑测试 | `tests/session_store.test.ts:363-377` | `bun test` | 沿 `parentId` 反向追溯并反转，正确重构分支的时序完整链路 | 🟢 验证通过 |
| **TM-SS08** | 环状引用死锁防御与安全熔断 (Seen Set Cycle Interruption) | 对抗性死锁防御 | `tests/session_store.test.ts:379-387` | `bun test` | `m1 <-> m2` 环路被 `seen.has` 捕获并立即安全熔断，杜绝调用栈溢出 | 🟢 验证通过 |
| **TM-SS09** | 外部磁盘增删物理扰动下的单一真理源自愈 (SSOT Reconcile) | 外部扰动测试 | `tests/session_store.test.ts:442-477` | `bun test` | 直接 `fs.unlink` 删除或外部写入新 `.jsonl`，`listSessions()` 自动校准同步缓存 | 🟢 验证通过 |
| **TM-SS10** | 官方 Claude Code 格式会话 100% 无缝读取恢复 | 真实互通性验证 | `tests/session_store.test.ts:506-568` | `bun test` | 直接加载官方 Claude Code 生成的 `.jsonl`，消息、DAG 链、标题完美复现 | 🟢 验证通过 |
| **TM-SS11** | 遗留 `{"t":"meta"}` 文件双向向下兼容 | 历史兼容性验证 | `tests/session_store.test.ts:570-605` | `bun test` | 平滑兼容历史旧版本格式文件，零阻断加载 | 🟢 验证通过 |
| **TM-SS12** | 官方全部 20 种 Entry 事件类型完整解析与元数据不丢失 | 1:1 Schema 覆盖 | `tests/session_store.test.ts:615-660` | `bun test` | 对齐 `D:/claude code/src/types/logs.ts:297-317` 全部 20 种 Entry 联合体类型，零丢失加载 | 🟢 验证通过 |
| **TM-SS13** | 节点分叉会话 (forkSession) 与 `forkedFrom` 溯源链持久化 | DAG 分叉端到端 | `tests/session_store.test.ts:662-690` | `bun test` | 从指定消息节点建立新独立会话，克隆前序祖先链，并持久化 `forkedFrom: { sessionId, messageUuid }` | 🟢 验证通过 |


| **TM-MSW1** | STYLEREF 域解析丢弃陈旧缓存并记录样式名（fldChar/fldSimple 双形态） | 单元+集成 | `tests/docx/styleref-dynamic-header.test.ts` | `bun test` | text 含 \uE002 标记、`styleRefs` 精确、缓存文字零残留 | 🟢 验证通过 |
| **TM-MSW2** | STYLEREF 保存回写完整域 span（begin/instr/separate/end，标记不泄漏） | 单元+集成 | `tests/docx/styleref-dynamic-header.test.ts` | `bun test` | 保存后 header XML 含 `STYLEREF "Heading 1"` 且无 \uE002 | 🟢 验证通过 |
| **TM-MSW3** | STYLEREF Word 三段搜索语义（页上首个/页前最近/页后首个/未知样式空白） | 单元测试 | `tests/docx/styleref-dynamic-header.test.ts` | `bun test` | 语义表全分支断言，含标题 N 与等级过滤 | 🟢 验证通过 |
| **TM-MSW4** | 逐页条带 makeGapHfEl 以活体标题文本替换标记 | DOM 集成 | `tests/docx/styleref-dynamic-header.test.ts` | `bun test` | 渲染文本含解析结果、标记字符零残留 | 🟢 验证通过 |
| **TM-MSW5** | 版面快照撤销逐字段还原（含保存路径依赖的 dirty 标志） | 单元测试 | `tests/docx/layout-undo.test.ts` | `bun test` | 还原快照与推入快照全字段相等 | 🟢 验证通过 |
| **TM-MSW6** | 版面撤销/重做对称栈与 Word redo 语义（新变更清空 redo、空栈安全） | 单元测试 | `tests/docx/layout-undo.test.ts` | `bun test` | undo→redo→undo 循环一致；push 后 canRedo=false | 🟢 验证通过 |
| **TM-MSW7** | Ctrl+Z 路由：仅当版面变更为最新动作时走版面栈，否则落 PM 历史 | 决策表单元 | `tests/docx/layout-undo.test.ts` | `bun test` | 真值表四分支（layout/doc/栈空/初始态）全覆盖 | 🟢 验证通过 |
| **TM-MSW8** | docs:zip-save 主进程注册、preload 绑定与桥类型声明 | 源码断言 | `tests/docx/zip-save-ipc.test.ts` | `bun test` | handler/preload/桥接口三处包含 zipSave 契约 | 🟢 验证通过 |
| **TM-MSW9** | IPC 路径与本地引擎产出 zip 逐条目字节等价（payload 自足性） | 等价性验证 | `tests/docx/zip-save-ipc.test.ts` | `bun test` | 条目键一致且每条目内容相等 | 🟢 验证通过 |
| **TM-MSW10** | 桥缺失/桥报错时等价回退本地引擎（保存永不因 IPC 失败而断） | 故障注入 | `tests/docx/zip-save-ipc.test.ts` | `bun test` | 两种故障下仍产出合法 docx 字节 | 🟢 验证通过 |
| **TM-P0-01** | zod→JSON Schema 类型保真：required/enum/integer/description 完整到达四种协议请求体（回归：曾全 string + required 恒空） | 单元+协议 | `tests/toolSchemas.test.ts` | `bun test` | 四协议请求体含 required 与真实类型；Gemini 大写类型且无非法关键字 | 🟢 验证通过 |
| **TM-P1-01** | 流式增量真实转发（回归：曾整轮一次性 yield） | 集成 | `tests/streamingExecution.test.ts` | `bun test` | 每个 content chunk 成为独立 message_delta 事件 | 🟢 验证通过 |
| **TM-P1-02** | 块级提前执行：tool_use 块完成即在流结束前开跑 | 集成 | `tests/streamingExecution.test.ts` | `bun test` | toolStartedAt < provider.streamEndedAt | 🟢 验证通过 |
| **TM-P1-03** | 到达序执行：非并发工具等待前面的并发工具 | 时序 | `tests/streamingExecution.test.ts` | `bun test` | tool_call_complete 顺序 = 到达顺序（正反两向用例） | 🟢 验证通过 |
| **TM-P1-04** | provider 块完成信号解析（content_block_stop / output_item.done / Gemini 整块） | 单元 | `tests/providerBlockCompletion.test.ts` | `bun test` | completedToolCalls 与最终 toolCalls 参数一致且 id 相同 | 🟢 验证通过 |
| **TM-P1-N1** | 负向：网络中断不产生悬挂 tool_use；非法 JSON 参数被 zod 拦截；幻觉工具名 fail-fast；重复块完成去重；同步 throw 捕获；级联 abort 连带排队工具 | 负向 | `tests/streamingNegative.test.ts` | `bun test` | 8 项故障面全部结构化收敛，无 crash 无挂死 | 🟢 验证通过 |
| **TM-P1-N2** | 负向：HTTP 500 / SSE 损坏行 / 重试耗尽 / 流中途断开 | 负向 | `tests/providerNegative.test.ts` | `bun test` | 错误含状态码与正文；损坏行被忽略；4 次尝试后抛错 | 🟢 验证通过 |
| **TM-P1-N3** | E2E 负向：幻觉工具名 / 沙箱路径逃逸拦截（回归：gate 曾漏读 filePath）/ 审批挂起后 abort | E2E | `tests/agentEngineNegative.test.ts` | `bun test` | 结构化错误回流模型；越界写被真实 SandboxGuard 拦截；abort 后状态干净可复跑 | 🟢 验证通过 |
| **TM-P2A-01** | ask/plan/bypass 三模式语义（ask=读+编辑放行、命令 ask；plan 仅只读） | 单元 | `tests/permissionModes.test.ts` | `bun test` | bypass 全放行；plan 拒写拒命令；未知模式 fail-closed 到 ask | 🟢 验证通过 |
| **TM-P2B-01** | checkPermissions deny/ask 门禁（合成工具；docx 试点语义另见 TM-RF-05） | 集成 | `tests/checkPermissions.test.ts` | `bun test` | deny 结构化错误回流且永不执行；ask 强制 HITL | 🟢 验证通过 |
| **TM-P2B-02** | 审批改参 updatedInput/userModified（含沙箱复检纵深防御） | 集成 | `tests/checkPermissions.test.ts` | `bun test` | 修改后参数执行 + 注记回流；改参为危险命令被复检拦截 | 🟢 验证通过 |
| **TM-P2B-03** | ApprovalCard 参数编辑（非法 JSON 拦截、未改动不误标） | 组件 | `tests/dom/ApprovalCard.domtest.tsx` | `vitest` | 非法 JSON/非对象拦截批准；编辑后携带 updatedInput；拒绝带理由 | 🟢 验证通过 |
| **TM-P3-01** | 动态 description 探针（默认 false / 抛错 fail-closed / provider 与 tool_search 联动） | 单元+协议 | `tests/dynamicDescription.test.ts` | `bun test` | docx 六工具 Live/Offline 两态描述真实到达 API；静态工具不受影响 | 🟢 验证通过 |

| **TM-MSW11** | STYLEREF 任意样式名解析（Title/自定义样式/中英文别名等价） | 单元测试 | `tests/docx/styleref-dynamic-header.test.ts` | `bun test` | 归一匹配全分支；未知样式空白 | 🟢 验证通过 |
| **TM-MSW12** | 版面撤销与打字在 PM History 中按时间序交织（真实编辑器） | 编辑器集成 | `tests/docx/layout-undo.test.ts` | `bun test` | undo 先撤文字再撤版面 B→A→空；redo 对称；attr-only 不动文本 | 🟢 验证通过 |
| **TM-MSW13** | zip-save 载荷过 structuredClone 后逐条目字节等价（Electron IPC 序列化链） | 等价性验证 | `tests/docx/zip-save-ipc.test.ts` | `bun test` | 克隆体与原对象产出 docx 条目全等 | 🟢 验证通过 |
| **TM-MSW14** | 词级对比导出含标准 w:ins/w:del/w:delText 且 unchanged 字节保真 | 端到端 | `tests/docx/compare-revision-export.test.ts` | `bun test` | 修订标记 + 作者属性 + 原文保真 + 无静默丢段 | 🟢 验证通过 |
| **TM-MSW15** | 打印预览页卡片离屏跳过渲染且打印强制全渲染 | CSS 契约 | `tests/docx/preview-virtualization.test.ts` | `bun test` | content-visibility + 精确 intrinsic 高度 + @media print 覆盖 | 🟢 验证通过 |
| **TM-MSW16** | gap 累加器二分实现与朴素扫描 20k 查询全等价（含重复 top/边界） | 等价+基准 | `tests/docx/gap-accumulator-perf.test.ts` | `bun test` | 逐值相等；合成基准快于朴素实现 | 🟢 验证通过 |

| **TM-MSW17** | 表格排序：数字列数值比较/文本码点比较/表头钉住/单步撤销 | 编辑器集成 | `tests/docx/table-sort.test.ts` | `bun test` | 升降序行序正确、表头未动、一次 Ctrl+Z 整表还原 | 🟢 验证通过 |
| **TM-MSW18** | 分栏分隔线 w:sep 解析/增删/round-trip 字节保持 + CSS/导出契约 | 引擎+契约 | `tests/docx/column-separator.test.ts` | `bun test` | 三态解析、翻转、未提供零改动、真实 docx 保留、column-rule 落地 | 🟢 验证通过 |
| **TM-MSW19** | autoSpaceDE/DN 独立解析/序列化与边界真值表（部分关闭不再压平） | 引擎+渲染 | `tests/docx/autospace-flags.test.ts` | `bun test` | DN-only-off 保真；仅 off 序列化；边界随 flags 增减 | 🟢 验证通过 |
| **TM-MSW20** | 表格公式 =SUM(ABOVE) 等方向/引用/范围/算术/掩码 + 表格级重算单撤销 | 引擎+集成 | `tests/docx/table-formula.test.ts` | `bun test` | 全分支求值、错误路径、真实表格写回 + 一次 Ctrl+Z 还原公式 | 🟢 验证通过 |


| **TM-RF-01** | 子代理门禁穿透：General 子代理危险工具经父级审批（无回调 fail-closed 拒绝）；Explore 只读不受影响（回归：曾以 bypass 静默执行） | 集成+负向 | `tests/reviewFixes.test.ts` | `bun test` | 无回调→自动拒绝零执行；批准→执行；拒绝→不执行；Explore 正常 | 🟢 验证通过 |
| **TM-RF-02** | HITL fail-closed：无审批回调时 requiresApproval 工具拒绝执行 | 集成 | `tests/reviewFixes.test.ts` | `bun test` | 结构化 fail-closed 错误、工具零执行、回合正常收尾 | 🟢 验证通过 |
| **TM-RF-03** | `list_directory` dirPath 沙箱链（回归：曾越狱枚举工作区外目录） | 安全 | `tests/reviewFixes.test.ts` | `bun test` | 工作区外 dirPath 被 SandboxGuard 拦截 | 🟢 验证通过 |
| **TM-RF-04** | 内置敏感写入规则：.env*/credentials* 写入 ask、读取放行、bypass 跳过 | 单元 | `tests/reviewFixes.test.ts` | `bun test` | 三态断言全对；非敏感写入不受牵连 | 🟢 验证通过 |
| **TM-RF-05** | docx 试点 checkPermissions 离线分支：apply_ops deny、modify_block allow（在线分支依赖画布，未经真机验证） | 单元 | `tests/reviewFixes.test.ts` | `bun test` | 离线行为与设计一致 | 🟢 验证通过 |
| **TM-RF-06** | userModified 注记每侧恰好一次（回归：模型侧曾重复两次） | 回归 | `tests/checkPermissions.test.ts` | `bun test` | 注记出现次数 == 1（事件侧与模型侧分别计数） | 🟢 验证通过 |

| **TM-R2-01** | Anthropic 三缓存断点：system 块 / 内置前缀末尾工具 / 最后消息块各带 cache_control；MCP 工具不带（负向：断点不得包含易变集） | 协议 | `tests/round2Alignment.test.ts` | `bun test` | 断点位置逐一断言；MCP 工具无标记；消息级标记恰好 1 个 | 🟢 验证通过 |
| **TM-R2-02** | 缓存前缀防漂移：相同输入两次请求 tools+system 序列化字节一致 | 协议 | `tests/round2Alignment.test.ts` | `bun test` | JSON.stringify 相等 | 🟢 验证通过 |
| **TM-R2-03** | 统一重试：Anthropic 503 / Gemini 429 → 4 次尝试 + statusUpdate；Responses 400 非瞬态立即抛（负向：不重试） | 负向/协议 | `tests/round2Alignment.test.ts` | `bun test` | 尝试次数与 statusUpdate 条数精确断言 | 🟢 验证通过 |
| **TM-R2-04** | 工具并发 env 覆盖：CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY 合法值生效、垃圾值回落默认（负向） | 单元 | `tests/round2Alignment.test.ts` | `bun test` | 4 生效；abc/-3/0/空 → 10 | 🟢 验证通过 |

| **TM-MSW21** | 审阅者集合过滤：作者去重、按集合接受/拒绝不伤他人修订 | 编辑器集成 | `tests/docx/reviewer-filter.test.ts` | `bun test` | 去重保序；空集零应用；单作者接受后他人标记原样 | 🟢 验证通过 |
| **TM-MSW22** | 九宫格对齐原子性：vAlign + 段落 align 单事务、null round-trip | 编辑器集成 | `tests/docx/cell-nine-align.test.ts` | `bun test` | 双属性同步写入、top/left 写 null、单步 Ctrl+Z 还原 | 🟢 验证通过 |
| **TM-MSW23** | 标尺单位刻度/换算/吸附真值 + 垂直标尺结构与撤销路径契约 | 纯函数+契约 | `tests/docx/ruler-units.test.ts` | `bun test` | inch/cm 刻度表、格式化、按单位吸附、VRuler 走 onSection 镜像 | 🟢 验证通过 |

| **TM-R3-01** | backfillObservableInput：事件/审批/规则匹配看到展开绝对路径，执行保留模型原始输入（1:1 Claude Code 观测/执行分离） | 集成 | `tests/round3Alignment.test.ts` | `bun test` | tool_call_start 与 approval_required 为绝对路径；execute 收到原相对路径；`~` 展开到主目录；command 等非路径键不展开（负向） | 🟢 验证通过 |
| **TM-R3-02** | LLM 记忆抽取：mode llm 合法 JSON → 保存 + 回调 + 类型归一（非法 type 过滤、字段截断、上限 3 条） | 集成 | `tests/round3Alignment.test.ts` | `bun test` | filename 前缀匹配 taxonomy；坏 type 被滤除 | 🟢 验证通过 |
| **TM-R3-03** | 负向：LLM 抛错 / 返回非 JSON → 静默回退正则启发式，回合不中断 | 负向 | `tests/round3Alignment.test.ts` | `bun test` | provider.callCount==1 且正则候选照常保存；垃圾输入零保存零 crash | 🟢 验证通过 |
| **TM-R3-04** | mode regex（直接构造默认）零 LLM 调用（保护既有测试契约与零成本场景）；createDefaultAgentEngine 默认 llm | 单元 | `tests/round3Alignment.test.ts` | `bun test` | provider.callCount==0；engine extractor mode=='llm' | 🟢 验证通过 |

| **TM-MSW24** | 格式匹配真值表（字段全匹配/未设通配/字体双槽/颜色#容差/三态 B-I） | 单元测试 | `tests/docx/format-find.test.ts` | `bun test` | 真值表全分支；范围收集/过滤/批量重涂单撤销 | 🟢 验证通过 |
| **TM-MSW25** | E 批集成契约：下沉导出 CSS 重发、小彩虹复用 WRAP_OPTIONS 同写路径、FindPanel 格式管线 | 源码契约 | `tests/docx/e-batch-contracts.test.ts` | `bun test` | 三个集成点逐一断言 | 🟢 验证通过 |

| **TM-R4-01** | 重试对齐 cc 请求级语义：CLAUDE_CODE_MAX_RETRIES 生效、默认 10 次、408/409 瞬态、x-should-retry 头双向（false 不重试/true 强制） | 协议/负向 | `tests/round2Alignment.test.ts` | `bun test` | 尝试次数精确断言（4/11/3/1） | 🟢 验证通过 |
| **TM-R4-02** | 级联合成错误（1:1 cc createSyntheticErrorMessage）：queued 兄弟获得"Cancelled: parallel tool call X errored"而非启动执行 | 集成 | `tests/streamingNegative.test.ts` | `bun test` | 排队工具零执行 + cc 文案断言 | 🟢 验证通过 |
| **TM-R4-03** | checkPermissions allow+updatedInput：权限层改写输入（沙箱复检、不误标 userModified） | 集成 | `tests/checkPermissions.test.ts` | `bun test` | 改写后参数执行；越界改写被复检拦截 | 🟢 验证通过 |
| **TM-R4-04** | E2E（真实 WS）：服务器网关审批流 deny（零副作用）/ approve+updatedInput（真实按改参执行） | **真实依赖 E2E** | `tests/e2e/serverGateway.e2e.test.ts` | `bun test tests/e2e/` | 真实 startServer + WS + 真实文件副作用断言 | 🟢 验证通过 |
| **TM-R4-05** | E2E（真实 LLM）：DeepSeek 生产同构配置 × 真实 write_to_file 副作用 | **真实依赖 E2E** | `tests/e2e/liveProvider.e2e.test.ts` | `bun test tests/e2e/`（有 key 才跑，无 key 显式 skip 记录） | terminal completed + 文件内容包含探针标记（实测 1.35s 通过） | 🟢 验证通过 |

| **TM-MSW26** | 版面撤销基线种子：首次版面变更 Ctrl+Z 回放原始版面（审查 F1 缺陷回归） | 编辑器集成 | `tests/docx/layout-undo.test.ts` | `bun test` | 非历史事务播种基线；undo 后 attr=baseline 且 watcher 回放；redo 对称 | 🟢 验证通过 |
| **TM-MSW27** | 对比导出整段删除携带段落标记删除（审查 F2 缺陷回归） | 引擎+导出 | `tests/docx/compare-revision-export.test.ts` | `bun test` | 被删段落 pPr 内含 w:rPr/w:del + 作者属性；Word 接受全部不留空壳 | 🟢 验证通过 |
| **TM-MSW28** | 表格排序并列稳定性：降序方向并列键保持原相对顺序（审查 F3 偏差回归） | 编辑器集成 | `tests/docx/table-sort.test.ts` | `bun test` | Y 最前、两个 X 并列 1-before-3 | 🟢 验证通过 |

| **TM-MSW29** | 显示层残缺回归：find-fmt 家族/modal 豁免/VRuler 主题变量/幽灵变量/九宫格九组合/rpr 过滤钩子/格式刷锁定态 | CSS 契约 | `tests/docx/display-fixes.test.ts` | `bun test` | 逐项断言规则存在且引用真实主题 token；不可能组合类保持清除 | 🟢 验证通过 |
| **TM-MSW30** | 段落标记 del 与既有 rPr 合并（CT_PPr 单 rPr 约束） | 引擎单元 | `tests/docx/compare-revision-export.test.ts` | `bun test` | 全文恰一个 w:rPr 且 w:del 先于 w:sz | 🟢 验证通过 |

| **TM-R5-01** | 工具正名对齐 cc：Bash/Read/Write/Edit/Glob/Grep/LS 为正名，旧名（run_command/view_file/…）全部经别名解析到同一对象 | 集成 | `tests/coreTools.test.ts` | `bun test` | getTool 旧名 === 新名对象；生产注册表含全部正名 | 🟢 验证通过 |
| **TM-R5-02** | 命令类判定 isCommandToolName 双名覆盖（正名+旧名），非命令工具不命中（负向） | 单元 | `tests/coreTools.test.ts` | `bun test` | 4 正名/旧名全 true；Read/Write 等 false | 🟢 验证通过 |
| **TM-R5-03** | 权限集合双覆盖：新名与旧名在 ask 模式下语义完全一致（回归：改名导致权限判定漂移的风险） | 回归 | `tests/coreTools.test.ts` | `bun test` | 7 组新旧名 behavior+requiresApproval 一致 | 🟢 验证通过 |
| **TM-R5-04** | TodoWrite：真实落盘 .nexus/todos.json + 摘要回显；负向（多 in_progress 拒绝且不落盘） | 单元/负向 | `tests/coreTools.test.ts` | `bun test` | 真实文件断言 + 状态机约束 | 🟢 验证通过 |
| **TM-R5-05** | WebFetch：HTML 去标签/实体解码/截断；负向（非 http(s) 拒绝、404 结构化错误） | 单元/负向 | `tests/coreTools.test.ts` | `bun test` | 正文提取断言 + script 剥离 + 错误码 | 🟢 验证通过 |
| **TM-R5-06** | WebSearch：DDG 解析含 uddg 解码与内联标签剥离；空结果提示（负向） | 单元/负向 | `tests/coreTools.test.ts` | `bun test` | 解码 URL 断言 + 空结果分支 | 🟢 验证通过 |

| **TM-MSW31** | 第三轮审查回归：modal label 特异性、esc 全局转义、compare-export/changed 样式、幽灵变量零残留 | CSS/源码契约 | `tests/docx/display-fixes.test.ts` | `bun test` | 限定选择器存在、/g 标志双替换、真实 token、九组合完备 | 🟢 验证通过 |

| **TM-R6-01** | NotebookEdit：.ipynb 单元 replace（code cell 执行状态重置）/insert（cellType 必填负向）/delete + cell 不存在结构化错误 + 非 .ipynb 拒绝 | 单元/负向 | `tests/r6Tools.test.ts` | `bun test` | 真实 .ipynb 文件读写断言；4 负向分支 | 🟢 验证通过 |
| **TM-R6-02** | AskUserQuestion：checkPermissions 恒 ask（交互在权限层）；answers 输出作答+notes 附加（附注不计入计数——真实计数 bug 修复）；无 answers 时提示自行决策不重试 | 集成/负向 | `tests/r6Tools.test.ts` | `bun test` | 作答文本 + guidance 分支 + bypass 自行决策提示 | 🟢 验证通过 |
| **TM-R6-03** | EnterPlanMode/ExitPlanMode：模式切换（plan→ask 恢复）、非 plan 拒绝（负向）、ExitPlanMode 恒 ask（计划必须经用户批准）、批准回显计划全文与 allowedPrompts | 集成/负向 | `tests/r6Tools.test.ts` | `bun test` | ctx 状态机断言 + 权限层 ask 断言 | 🟢 验证通过 |
| **TM-R6-04** | Skill：.agents/skills/<name>/SKILL.md 加载、$ARGUMENTS 替换、frontmatter 剥离；负向：未知 skill 列出可用清单 | 单元/负向 | `tests/r6Tools.test.ts` | `bun test` | 真实 SKILL.md 文件加载断言 | 🟢 验证通过 |
| **TM-R6-05** | Task 系列（真实后台进程）：TaskCreate 真实 spawn → TaskOutput 读真实输出（等待 exit 事件）→ TaskStop 真实终止长任务；负向：不存在 id 列表回显 | 集成 | `tests/r6Tools.test.ts` | `bun test` | 真实进程输出/退出码/终止断言 | 🟢 验证通过 |
| **TM-R6-06** | MCP 客户端：wrapMcpTool isError 透传/content 拼接/空结果/passthrough schema；connectMcpServers 无配置零连接 + 缺 command failed（负向） | 单元/负向 | `tests/r6Tools.test.ts` | `bun test` | mcp__ 前缀/isMcp/mcpInfo 断言；失败归类断言 | 🟢 验证通过 |

| **TM-E2E-R** | 真实 Electron 启动：真二进制 + 真构建产物 + renderer 真实加载 + 无崩溃 | 真机冒烟 E2E | `scripts/e2e-app-boot.mjs` | `node scripts/e2e-app-boot.mjs` | PASS：CDP 见 renderer 页面、无 JS error、进程收尾 | 🟢 验证通过 |
| **TM-DOM-1** | 小彩虹弹层真实交互：选中图片→点选环绕→节点 attr 真实写入（focus 退化缺陷回归） | DOM 行为 | `tests/dom/wordDisplay.domtest.tsx` | `npx vitest run tests/dom` | NodeSelection 下 apply 后 imageWrap 真实变更 | 🟢 验证通过 |
| **TM-DOM-2** | FindPanel 格式过滤收敛 + 替换带格式落色 | DOM 行为 | `tests/dom/wordDisplay.domtest.tsx` | `npx vitest run tests/dom` | 1/3→1/2 过滤；替换段新色、未替换段保原色 | 🟢 验证通过 |
| **TM-DOM-3** | 简单标记 ORIGINAL 视图装饰还原修订前版式 | 编辑器+DOM | `tests/docx/simple-markup-mode.test.ts` | `bun test` | style 含还原的 text-align/margin；ALL 视图消失 | 🟢 验证通过 |
| **TM-DOC-4** | zip-save 生产 handler 函数直调与引擎逐字节一致 | 生产行为 | `tests/docx/zip-save-ipc.test.ts` | `bun test` | document.xml 全等 | 🟢 验证通过 |

| **TM-R7-01** | Edit 参数对齐 cc：old_string/new_string/replace_all（旧名 targetContent/replacementContent/allowMultiple 经兼容层归一） | 集成/回归 | `tests/readFirstAndCron.test.ts` + 既有套件 | `bun test` | cc 训练分布参数直用；旧名兼容不破坏存量会话 | 🟢 验证通过 |
| **TM-R7-02** | Read 参数对齐：offset/limit（数量语义，旧 startLine/endLine 归一）+ cat -n 行格式 + 256KB/25K token 双上限 + 二进制拦截 + 空文件/超界 system-reminder | 集成/负向 | `tests/fileTools.test.ts` | `bun test` | 格式与 cc 一致；超限错误教模型用 offset/limit 自救 | 🟢 验证通过 |
| **TM-R7-03** | read-before-write 状态机（数据安全级）：Write/Edit 对已存在文件强制先 Read + mtime 漂移拒绝（±2s 容差） | **安全** | `tests/fileTools.test.ts` 等 | `bun test` | 未读先写/陈旧写均被 cc 文案拒绝；新建文件不受影响 | 🟢 验证通过 |
| **TM-R7-04** | 错误自诊链：ENOENT 时 "Did you mean <相似文件>"（findSimilarFile 移植） | 单元 | `tests/readFirstAndCron.test.ts` | `bun test` | 错误含相似文件建议 | 🟢 验证通过 |
| **TM-R7-05** | Bash 对齐：timeout 参数（默认 120s/上限 600s/env）、非零退出进错误通道、大输出落盘可回读（30K 阈值）、run_in_background（转 Task 注册表）、description/dangerouslyDisableSandbox 参数 | 集成 | `tests/reviewFixes.test.ts` 等 | `bun test` | 超时/退出码行为断言；后台任务 ID 返回 | 🟢 验证通过 |
| **TM-R7-06** | Glob：mtime 降序（最新优先）+ cc 截断文案 + path 不存在校验（负向） | 集成/负向 | `tests/orchestrator.test.ts` 等 | `bun test` | 排序断言 + "(Results are truncated...)" 教模型收紧 | 🟢 验证通过 |
| **TM-R7-07** | Grep 全参数面：output_mode（files_with_matches 默认/content/count）/ -i / -n / -B/-A/-C / head_limit/offset / content 保留缩进 + 500 列截断 | 集成/负向 | `tests/orchestrator.test.ts` 等 | `bun test` | 三种模式输出契约；count 汇总；分页语义 | 🟢 验证通过 |
| **TM-R7-08** | Cron 四件套：CronCreate/List/Delete/Update + 5 字段校验（负向：范围/格式/步进）+ 到点触发 + 55s 去重 | 单元/集成/负向 | `tests/readFirstAndCron.test.ts` | `bun test` | 非法 schedule 拒绝；匹配触发；busy 跳过 | 🟢 验证通过 |
| **TM-R7-09** | AskUserQuestion UI：ApprovalCard 渲染 questions/options，全答才可 Submit，答案经 updatedInput.answers 回填（updatedInput 为整体替换，必须携带完整原参数） | 组件/判别 | `tests/dom/ApprovalCardAskQuestion.domtest.tsx` | `vitest` | 渲染断言 + updatedInput 含 questions 与 answers | 🟢 验证通过（补齐实测用例，此前虚报已更正） |
| **TM-R7-10** | TodoWrite 对齐 cc：activeForm 必填、固定确认语（不回显列表）、全部 completed 清空存储 | 单元/负向 | `tests/coreTools.test.ts` | `bun test` | 落盘断言 + 确认语断言 | 🟢 验证通过 |
