# 交接手册: 交接与恢复状态说明书

> **本文档是 agent 中断/上下文重置后的唯一恢复入口**（evidence-driven-engineering 规范）：下一 agent 必须能仅凭本文档与证据链恢复，不能猜测进度。
> 最后更新: 2026-09-20（三轮对齐 + R4 传输层/真实性收口时点）

---

## 1. 当前状态快照

- **分支**: `feat/claude-code-tool-system-alignment`（已推送，PR #3 → main）
- **关键提交**: `0b35381`（docs 中文化 + 记录收口）、`5af1cdb`（三轮对齐代码）、R4 批次（cc 重试语义对齐 + 真实 E2E）
- **门禁实测（2026-09-20）**: `bun test tests/` 全绿（0 fail，用例数随并行会话增长，提交时点 663+）；`npx vitest run` 全绿；双端 `tsc --noEmit` exit 0。**0 fail 是硬性结论**。
- **真实 E2E**: 服务器网关审批流（真实 WS + 真实 query 循环）deny/approve+updatedInput 两场景通过；真实 LLM（DeepSeek，生产同构配置）× 真实 write_to_file 副作用 E2E 通过。
- **PR 注意**: PR #3 含此前未提交的基础批次与并行会话的 Word/workspace WIP（已被 tracked 代码引用无法剥离，PR 描述已披露）。

## 2. 系统核心入口

| 模式 | 启动命令 | 入口文件 |
| :--- | :--- | :--- |
| CLI 命令行 | `bun run bin/agent-cli` | `bin/agent-cli` |
| 本地 Server Sidecar | `bun run src/server/index.ts` | `src/server/index.ts`（`startServer(port, host)`） |
| 主门禁测试 | `bun test tests/` | `tests/`（600+ 用例）+ `npx vitest run`（DOM 套件 `tests/dom/`） |
| 桌面客户端开发态 | `npm run dev` | `src/renderer/` + `src/main/` |
| 全端生产打包 | `npm run build` | electron-vite → `out/` |
| 真实依赖 E2E | `bun test tests/e2e/` | 服务器网关 WS 审批流 + 真实 LLM（无 key 自动跳过并记录） |

## 3. 已完成能力与真实入口（勿重复实现、勿凭记忆猜路径）

| 能力 | 入口 |
| :--- | :--- |
| 工具 schema 序列化（zod→JSON Schema + WeakMap 缓存） | `src/main/agent/utils/toolSchemas.ts` |
| 流式工具执行器（块级完成/gate/到达序/级联合成错误） | `src/agent/core/StreamingToolExecutor.ts` + `src/agent/core/query.ts` |
| 权限引擎（ask/plan/bypass 三模式 + 敏感写入内置规则） | `src/main/agent/permissions/PermissionEngine.ts` |
| 子代理（继承父级门禁；无回调 fail-closed） | `src/main/agent/subagents/SubagentEngine.ts` |
| 沙箱守卫（路径监狱 + 命令黑名单） | `src/main/agent/sandbox/SandboxGuard.ts` |
| 斜杠命令（/clear /memory /compact /undo /help） | `src/main/agent/commands/` |
| 会话存储（JSONL 追加 + fork 分支树） | `src/main/session/sessionStore.ts` |
| 记忆系统（Manager / Extractor llm+regex / Compactor） | `src/main/agent/memory/` |
| Provider（4 协议 + cc 语义统一重试 + Anthropic 三缓存断点） | `src/main/agent/providers/` + `src/main/agent/utils/providerHttp.ts` |
| 动态工具描述探针 | `src/main/agent/utils/runtimeContext.ts` |
| Word 工作台与 docx 双通道执行 | `src/renderer/src/components/word/` + `src/main/agent/tools/docxTools.ts`（并行会话持续开发中） |

## 4. 三轮对齐记录入口

1. 结构语义 P0–P3：`docs/Claude-Code工具系统分析.md` §9
2. 独立审查修复：`docs/证据链.md` §15.5
3. 传输层 R2（缓存/重试/并发 env）：分析文档 §10、证据链 §15.6
4. 观测分离 + LLM 记忆 R3：分析文档 §11、证据链 §15.7
5. R4（cc 重试语义对齐 + 真实 E2E）：分析文档 §12、证据链 §15.8
6. **收口报告（最终诚实报告）**：分析文档 §12

## 5. 剩余未对齐项（新工作起点）

分析文档 §10.4：interruptBehavior 插话语义、Provider 预设生态与协议转换代理、会话 SQLite 全文索引、OS 级沙箱；LLM 记忆的**相关记忆预取与团队记忆同步**（R3 只交付抽取）。

## 6. 在途并行工作警示

存在并行会话的未提交/在途修改（Word UI、workspace 多 Pane、DeltaCoalescer 合帧器及契约文档 CONTRACT-P1-S*）。**接手 agent 不要把它们当作本分支交付物评审，也不要回滚**；按并行 WIP 对待。

---

## 历史版本说明（不可删除）

本文档 2026-09-17 版描述「7 项核心架构测试」、建议「实现 subagents/compaction」——**上述能力均已交付**（`src/main/agent/subagents/`、`src/main/agent/memory/ContextCompactor.ts`），旧建议勿再执行；测试规模现为主门禁 600+ 用例。取代原因：三轮对齐批次（2026-09-20）交付了旧版建议清单中的全部内容，详见 `docs/进度.md` 与 `docs/证据链.md`。
